(function(root){

    {{{code}}}

    if (typeof define === 'function' && define.amd) {
        define([], function(){
            return {{name}};
        });
    } else if (typeof exports === 'object') {
        module.exports = {{name}};
    } else {
        root.{{name}}  = root.{{name}} || {{name}};
    };

}(this));
